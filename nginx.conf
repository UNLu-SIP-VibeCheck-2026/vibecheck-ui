server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Esto es el fallback del router que pide la guía:
        try_files $uri $uri/ /index.html;
    }
}